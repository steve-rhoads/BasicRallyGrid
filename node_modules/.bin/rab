#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../rally-app-builder/bin/rally-app-builder" "$@"
  ret=$?
else 
  node  "$basedir/../rally-app-builder/bin/rally-app-builder" "$@"
  ret=$?
fi
exit $ret
